# Roadmap
- [x] Android keyboard gap: native single inset owner plugin (aelixto-window-insets)
- [x] Link-box close flicker: keyboard overlay mode while the box is open
- [ ] User: `npm install && npx cap sync android`, rebuild, verify with Layout debug
